package com.mkisten.vacancybackend.repository;

import com.mkisten.vacancybackend.entity.AiResumeAdminSettings;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AiResumeAdminSettingsRepository extends JpaRepository<AiResumeAdminSettings, Long> {
}
